/** To start, we will only model the local temperature,
    more detailed region-wide weather model will come later. */
var Weather = {
  temperature: function(elevation, hr, day of year) {
    return 13;
  }
}
